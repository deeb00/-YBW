<%= form_for(@importance) do |f| %>
  <% if @importance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@importance.errors.count, "error") %> prohibited this importance from being saved:</h2>

      <ul>
      <% @importance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
